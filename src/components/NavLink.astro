---
const { link, text, colour} = Astro.props;
---

<li style={`--bullet-colour:${colour};`}>
  <a
    href={`${import.meta.env.BASE_URL}${link}`}
    class="block rounded-md text-xl hover:text-blue-500 transition duration-200"
    >{text}</a>
</li>
